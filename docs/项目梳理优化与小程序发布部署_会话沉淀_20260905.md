# 项目梳理优化与小程序发布部署 · 会话沉淀

> 2026-09-05 · 会话主线：全仓扫描与优化计划 → W1 执行与 LLM 盲审 → 小程序发布线推进 → 云服务器首次部署实战
> 配套文档：[`docs/plans/optimization-plan-2026-09.md`](./plans/optimization-plan-2026-09.md)（项目级优化总盘，本会话核心产出）、[`docs/release/server-deploy-runbook.md`](./release/server-deploy-runbook.md)、[`docs/release/submission-materials.md`](./release/submission-materials.md)

## 一、会话概览

| 阶段 | 触发 | 结果 |
|---|---|---|
| ① 全仓扫描与梳理 | 用户要求扫描项目、梳理已有 md 文档 | 3 个探索代理扫 docs/ / xingce-structured-data / 代码结构；产出文档地图+时效分层+6 项不一致清单 |
| ② 优化计划与执行 | 用户要求按梳理规划优化计划并执行 | 沉淀 `optimization-plan-2026-09.md`（6 工作流×3 阶段）；阶段 0（W1 文档治理 6 项）全部完成；DeepSeek key 验证可用并补跑言语 LLM 盲审（39/40） |
| ③ 小程序发布线 | 用户聚焦小程序发布（备案等待期并行准备） | 禁词扫描+改名、legal 三处填写、开放注册配套、提审材料包、服务器核查脚本；微信后台类目/名称/头像由用户完成 |
| ④ 云服务器首次部署 | 用户开始部署 | 实战暴露 npm/apt 镜像源问题并修复推送；构建"挂死"待定性（用户充值中）；runbook 沉淀 |

## 二、关键决策记录

1. **DeepSeek key 管理约定**：key 存 `secret/dk-key`，**任何会话不得读入上下文**，脚本/环境变量直接使用；`secret/` 已加入 `.gitignore` 并验证 git 不跟踪。已就地写入 `server/.env` 的 `LLM_API_KEY`（盲审脚本从该文件读取 `LLM_ENABLED/LLM_API_KEY/LLM_BASE_URL/LLM_MODEL`），实测 HTTP 200（deepseek-v4-flash）。
2. **文档治理策略**：历史战略文档加"⚠️ 历史文档"横幅并重定向现行入口（roadmap + optimization-plan），而非删除；CLAUDE.md 整体同步 AGENTS.md（以 AGENTS.md 为准）。
3. **`.bak` 备份处置**：确认 D6a/D7/D8-D9 数据操作验收后，三份备份（约 31MB）先 tar 归档至工作区外（`~/Projects/zhixing-gongkao-backups/xingce-2025-bak-d6a-d7-d8d9-20260905.tar.gz`，77 项校验）再删除，DATA_STATUS 两处登记。
4. **提审口径统一**：theory-app 界面「政治理论」→「理论学习」（今日/学习页导航标题、我的页标题、项目描述共 4 处），与已过敏感词扫描的简介用词一致；legal 免责声明中的"培训/考试/课程"命中为**主动定性话术，保留不改**。
5. **legal 三处定稿**：署名「宁易」、邮箱 `dnegningning163@163.com`（⚠️ 疑似拼写有误 dnegn/dengn，用户复核前不要提审）、公众号「杜衡阁」、生效日期 2026-09-05；隐私政策「成年备考人群」→「成年自学人群」。
6. **注册策略拍板：开放注册**（`ALLOW_REGISTER=true`）。配套最小找回路径：两 app 登录页新增「忘记密码？点此复制邮箱人工重置」（复用 `LEGAL_CONTACT.email`，`Taro.setClipboardData` 只写不读，与隐私声明一致），版本描述补充找回说明。
7. **部署修复策略**：构建镜像源修复进 Dockerfile 并推送 `origin/main`（`bb29d1f`），服务器侧只手动打过 npm 两行 sed，充值回来后 `git checkout -- Dockerfile && git pull` 对齐，消灭补丁漂移。
8. **SSH 远程操作纪律**：多行 heredoc/长粘贴在 SSH 终端会被截断（本次实测复现），一律改**单行命令**；需要传文件用 `base64 -d > file`。

## 三、完成事项明细

### W1 文档与仓库治理（optimization-plan W1，6/6 ✅）

- CLAUDE.md 同步 AGENTS.md（含头部"以 AGENTS.md 为准"说明）
- PROGRESS.md 回填 08-31~09-05：新增 7 条阶段记录（schema v2 / 答案接入 / S1 报告 / 出题引擎 Q2-Q6+137 题 / 题库持久化 P0-P2 / 每日出题线 / 优化计划），规模快照更新（页面 53→60），最近提交刷新，§6 补 roadmap 与 optimization-plan 链接
- 四份根目录战略文档加历史横幅；README/PROGRESS 引用改指现行计划
- `question-bank-persistence-admin-design.md` 头部改为"P0-P2 已落地（09-03），P3/P4 待实施"
- `.bak` 三份归档删除（见决策 3）；导出三件套重导（server venv 补装 openpyxl，xlsx/Anki/Markdown 全部反映 2024/2023 答案），`export_questions.py` 过时 docstring 修正

### 出题线（W3.2 提前完成）

- 重跑 `gen_verbal_questions_v2.py`：**40 题 LLM 盲审 39/40 一致（97%）**——多空选词 9/10，唯一不一致 `Q5-VB-MULTI-005`（正确 C / LLM 选 B）被报告标记疑似歧义题，转入人工复核队列（W3.5 U 类缺陷）；判断等其余引擎无现成 LLM 盲审模块，接入留后续

### 小程序发布线（W5）

- 禁词子串扫描：双 app 源码+dist 全量（product-copy §6 五类禁词 + 政治/公考/备考/政考），唯一改动即决策 4；产物用生产 API 重建并验证
- legal 填写 + 忘记密码入口（决策 5/6），两 app 重建验证：`待填` 零命中、宁易/杜衡阁/邮箱/生效日期均编译进 `common.js`（⚠️ dist 内中文以 `\uXXXX` 转义存在，验证需按转义形式 grep）
- 产出 [submission-materials.md](./release/submission-materials.md)：双 app 简介（主推+精简）/详细描述/标签/类目建议/版本描述模板（含找回说明）/截图清单/核对单；体验账号两处【待填】
- 用户侧完成：微信后台类目确认（B4）、名称核验（B5）、头像（B3 部分）；launch-plan B 表已同步
- 新增 [server/scripts/launch_readiness_check.py](../server/scripts/launch_readiness_check.py)：只读核查（内容池家底/旧昵称残留/.env 关键变量/网关 /docs 收口），修过 SQLite 双引号标识符当字符串字面量返回假分布的坑（先 pragma table_info 再查列）
- 本地试跑重要更正：`zhixing.db` **不是空库**（此前统计脚本 glob 误取空的 `app.db`）——含 2025 三卷 395 题位、284 question_items、6 个发布包等 dev 数据；生产库家底仍需上服务器跑脚本确认

### 云服务器首次部署（新战区）

- 确认服务器为**全新未部署**状态（/opt 无仓库），走完整初始化：git/deploy key/clone/setup-docker/.env/deploy.sh
- 实战修复（已进 Dockerfile 并推送 `bb29d1f`）：npm 构建走 npmmirror（修复后 admin `npm ci` 19.1s，实证有效）；apt 换 mirrors.aliyun.com
- 待定性：`vue-tsc` 20min+ 静默 / apt 爬行——嫌疑① docker bridge 网络 MTU/DNS 死连接（修复预案：daemon.json 合并 `mtu:1400` + 国内 DNS，**须保留 setup-docker.sh 写入的 registry-mirrors，不可直接覆盖**）；嫌疑② 云账户欠费/带宽限制（用户充值中，充值本身就是验证）
- 沉淀 [server-deploy-runbook.md](./release/server-deploy-runbook.md)：初始化 8 步、故障排查表（6 症状）、判活三件套（`ps aux --sort=-%cpu` / `free -h` / `/proc/net/dev` 两次采样）、中期优化路线（ACR 预构建 / CI 构建 / SKIP_TYPECHECK / 构建期临时升带宽）

## 四、产出物清单

| 类型 | 文件 |
|---|---|
| 计划 | `docs/plans/optimization-plan-2026-09.md`（含 §5 执行记录，随做随勾） |
| 发布 | `docs/release/submission-materials.md`、`docs/release/server-deploy-runbook.md`、`server/scripts/launch_readiness_check.py` |
| 代码 | `apps/{shenlun,theory}-app/src/constants/legal.ts`（填值+自学人群）、`apps/*/src/pages/auth/login.vue`（忘记密码入口）、theory 4 处改名、`.gitignore`（/secret/）、`Dockerfile`（镜像源，已推送） |
| 文档更新 | CLAUDE.md、PROGRESS.md、README.md、4 份战略文档、launch-plan、persistence design、DATA_STATUS、CHANGELOG、`export_questions.py` docstring |
| 数据 | export 三件套重导（xlsx/Anki/Markdown×16）、`generated/qa_verbal_v2.{json,report.md}`（盲审版） |
| 归档 | `~/Projects/zhixing-gongkao-backups/xingce-2025-bak-d6a-d7-d8d9-20260905.tar.gz` |

## 五、遗留与下一步

| # | 事项 | 等待点 |
|---|---|---|
| 1 | 服务器部署收口 | 用户充值 → 诊断三件套输出发作者 → 定性挂死（MTU/DNS vs 带宽）→ `git checkout -- Dockerfile && git pull` → deploy.sh → health → 建号 |
| 2 | **邮箱复核**：`dnegningning163@163.com` 疑似拼写错误 | 用户确认；错则改 `legal.ts`×2 + 重建 |
| 3 | 体验账号账密 → 填 submission-materials §3 两处【待填】 | 部署成功后注册 |
| 4 | **生产内容池导入（空库）**：真题 2025 三卷 / 137 生成题（还全在 pending_review）/ 权威文章 | 部署成功后，按 readiness 输出定缺口 |
| 5 | 备案通过 → certbot → nginx 网关 → 微信合法域名 → 真机回归 → 提审 | 外部 |
| 6 | 隐私指引勾选（legal-draft 映射表） | 用户微信后台 |
| 7 | 工作区变更分组提交（见 §六） | 用户点头 |

## 六、git 状态与本会话提交对照

**已推送**：`bb29d1f fix(docker): 构建内 npm 走 npmmirror、apt 换阿里云源…`（注意：本会话发现仓库 HEAD 在 `feat/qb-pipeline` 分支而非 main，修复经 `git push origin HEAD:main` 挂上 origin/main，本地 main 已 fast-forward 同步）。

**工作区未提交（本会话）**：按组提交建议——

1. `docs(xingce): W1 文档治理`：CLAUDE.md、README、PROGRESS、4 战略文档、persistence design、DATA_STATUS、CHANGELOG、export 三件套、export_questions.py、generated/qa_verbal_v2*
2. `feat(apps): 提审合规（legal 填写/理论学习改名/忘记密码）`：apps/ 下 10 个文件
3. `docs(release): 提审材料包+部署 runbook+优化计划`：docs/plans、docs/release ×3、server/scripts/launch_readiness_check.py、.gitignore、launch-plan

**⚠️ 工作区还存在非本会话的并行改动**（另一会话产出，勿与上述混提）：`xingce-structured-data/2026/`（新目录，含 _extract/答案决策/媒体裁图）、2020-2022 `papers/*.json`、`sync_status.py`、`_schema/topic-vocabulary.json`、`catalog.json`——提交前先与对应会话确认状态。

## 七、本会话 commit 对照

| commit | 内容 |
|---|---|
| `bb29d1f` | fix(docker): 构建内 npm 走 npmmirror、apt 换阿里云源（已上 origin/main） |
| （本次沉淀） | docs: 本会话沉淀文档 |
