# 内容运营驱动产品实施归档

> 建档日期：2026-09-07
> 适用产品：知行日知（时政学习）、知行策论（申论学习）
> 依据方案：`docs/products/content-led-launch-design.md`

## 1. 固定执行规程

每个开发单元严格按以下顺序推进：

1. 明确本单元目标、边界和验收条件。
2. 实施代码及必要文档修改，不混入其他未完成工作。
3. 完成自动化测试、静态检查和 H5／小程序构建等相应验证。
4. 在本文追加实施结果、实际能力、限制、验证证据和下一项。
5. 创建独立 Git 提交并推送到已确认远端。
6. 推送成功后自动进入下一项；遇到发布、密钥、外部发送或其他需要新增授权的动作时暂停确认。

“完成”只表示归档中列出的验收已通过，不代表已经部署、提审或公开运营。规划功能、代码功能、部署状态和平台发布状态分别记录。

## 2. 开发单元归档

### U01：时政文章免费学习闭环

- 状态：本地开发、验证、归档、提交与 GitHub 推送均已完成。
- 完成日期：2026-09-07。
- 本地提交：`0a4ffaa feat(theory): add guest article learning flow`。
- 产品范围：知行日知 H5、微信小程序、共用 FastAPI 后端。

#### 实际交付

- 未登录用户可以免费打开时政文章，选择“阅读全文”或“读重点”。
- 同一文章的已审核题目按每辑最多 5 题分组；题库完整时可以选择全文题目合集。
- 分辑与合集引用相同题目 ID，避免重复创建题目和错题身份。
- 单选、判断题点击后提交；多选题明确提交后才判定。
- 题面接口不返回答案、解析和原文依据；提交单题后才返回对应反馈。
- 游客作答及多选草稿按内容版本保存在当前设备；切换分辑、退出再进可以恢复。
- 内容版本变化后，旧页面停止继续判题并提示重新打开，避免新旧答案混判。
- 下线文章、未开放练习、待审核题和不完整题目不会进入公开合集。
- 今日页和复习页向游客提供文章学习入口，不再强制首次访问登录。
- 对外界面统一使用“时政学习”，产品名保持“知行日知”，主题配置统一为红色。

#### 数据与接口

- `GET /api/learning/articles/{article_id}`：返回已发布文章、无答案题面、分辑和内容版本。
- `POST /api/learning/articles/{article_id}/check`：校验内容版本、题目归属和选项后返回单题反馈。
- 两接口只允许 `X-Product-Key: theory`，不写账号积分、错题或阅读计数。
- 本地游客存储键包含文章 ID 和内容版本；当前尚未实现登录后的跨端合并。

#### 验证证据

- 后端相关测试：17 项通过，包括匿名分辑、答案隐藏、非法答案、跨产品隔离、内容下线和版本冲突。
- TypeScript：`tsc --noEmit -p apps/theory-app/tsconfig.json` 通过。
- ESLint：本单元相关页面、组件与 API 文件无错误。
- H5：`apps/theory-app` 的 `npm run build:h5` 通过。
- 微信小程序：`apps/theory-app` 的 `npm run build:weapp` 通过。
- 构建存在 Sass `@import` 和 legacy JS API 的上游弃用提示，不影响本次产物生成，后续作为工程升级事项处理。

#### 明确限制

- 游客记录目前只在本机保存，清理浏览器／小程序缓存会丢失；还不能登录后合并到账号。
- “读重点”暂使用文章已审核摘要，还没有独立的段落级重点标注结构。
- 当前分辑按稳定题目顺序每 5 题切分；运营后台还不能人工调整分辑、固定发布版本或标记长期重点专题。
- 尚未生成公众号、小红书、知乎、视频号的正式入口和运营发布包。
- 尚未部署、提审或公开发布。

#### 下一开发单元

`U02：申论学习公共三刀示范＋免费短练习闭环`。

目标是把已审核的三刀解剖作为公共教学示范展示，与用户个人练习隔离；未登录用户可阅读、完成短练习并在本机保存，不承诺 AI 评分。完成后按同样规程验证、归档、提交并推送。

### U02：申论学习公共三刀示范＋免费短练习闭环

- 状态：本地开发、验证、归档、提交与 GitHub 推送均已完成。
- 完成日期：2026-09-07。
- 提交：`96d9292 feat(shenlun): add guest three-cut learning flow`。
- 产品范围：知行策论 H5、微信小程序、共用 FastAPI 后端。

#### 实际交付

- 未登录用户可从“今日”或“精读”进入免费学习，不再因首次访问被强制送往登录页。
- 公共学习路径固定为三步：读原文、看三刀、做短练习；游客只看到当前已经开放的简单闭环。
- 公共示范沿用原版三刀字段和顺序：论证骨架包含总论点、分论点、论据、小结、论证方法、方法说明、套用模板与总结；规范表达包含规范词、经典金句和高频动词；万能句式包含原句、模板与仿写。
- 短练习支持字数范围、提交前自查、人工参考答案对照；未达到字数或未完成自查时不显示参考答案。
- 页面明确说明参考答案是教研对照而非 AI 评分，避免形成尚未实现的批改承诺。
- 游客答案、自查状态和是否已查看参考按文章与内容版本保存在当前设备；内容改版不会误恢复到新版本。
- 登录用户原有文章详情、复制全文、语料收集、个人三刀编辑器和个人记录仍保留；公共示范没有取代或改写原功能。
- 第三个底部页签的对外文案由“训练”统一为“练习”，与四栏统一导航规范一致。

#### 数据与接口

- 新增 `shenlun_teaching_examples` 公共教研示范表，按文章与版本唯一；它与含 `user_id` 的 `shenlun_mine_logs` 个人开采表严格分离。
- `GET /api/shenlun/learning/articles`：匿名返回具有完整已发布教研示范的文章列表。
- `GET /api/shenlun/learning/articles/{article_id}`：匿名返回原文、三刀示范、短练习和内容修订指纹。
- 两个接口只允许 `X-Product-Key: shenlun`；读取和本机练习都不会创建个人开采记录，也不会增加文章阅读计数。
- 草稿、下线文章、残缺论证点、空规范词、残缺句式或缺少短练习参考的内容均按失败关闭原则不进入前台。
- 新增 `server/scripts/import_shenlun_teaching_example.py`：从人工审核 JSON 导入，默认只建草稿；必须显式传入 `--publish` 才发布。
- 同一文章同一版本不允许用不同内容或状态覆盖；内容有变化必须升版本，避免线上教研内容被静默改写。
- 导入字段契约示例位于 `server/scripts/examples/shenlun_teaching_example.json`。

#### 验证证据

- 申论公共接口专项测试：12 项通过，覆盖匿名读取、原版字段、草稿与残缺数据拦截、文章下线、跨产品隔离、无个人记录副作用和版本指纹。
- 后端全量测试：158 项通过；现有 135 条警告来自 JWT 依赖的 UTC 时间弃用提示，不是本单元新增失败。
- TypeScript：`tsc --noEmit -p apps/shenlun-app/tsconfig.json` 通过。
- ESLint：本单元相关页面、路由配置与 API 文件无错误。
- H5：`apps/shenlun-app` 的 `npm run build:h5` 通过。
- 微信小程序：`apps/shenlun-app` 的 `npm run build:weapp` 通过。
- 双端构建仍有 Sass `@import` 和 legacy JS API 弃用提示，不影响本次产物。

#### 明确限制

- 本单元没有把云盘中的现有运营稿自动发布为公共教研示范；需按 JSON 契约人工复核并显式发布后，前台才会出现对应内容。
- 游客短练习只保存在本机，清理缓存或更换设备会丢失；尚未支持登录后合并与跨端同步。
- 当前公共短练习每篇一题，只提供结构化自查和人工参考，不含 AI 批改、分数或个性化建议。
- 公共教研示范目前通过安全导入命令维护，管理后台尚未提供可视化编辑、预览、审核与发布功能。
- 尚未部署、提审或生成带小程序码的渠道发布包。

#### 下一开发单元

`U03：游客学习记录登录合并与跨端同步`。

目标是为时政答题和申论短练习建立统一、可追溯的游客记录迁移协议；只有用户主动登录后才上传，服务端幂等合并，保留本机来源与内容版本。完成后按同样规程验证、归档、提交并推送。

### U03：游客学习记录登录合并与跨端同步

- 状态：本地开发、验证、归档、提交与 GitHub 推送均已完成。
- 完成日期：2026-09-07。
- 提交：`67f41f1 feat(learning): sync guest progress after login`。
- 产品范围：知行日知、知行策论 H5 与微信小程序、共用 FastAPI 后端。

#### 实际交付

- 时政文章游客答题和申论游客短练习共用一套服务端同步协议，但按产品严格隔离记录类型。
- 用户未登录时仍只写当前设备；完成登录或注册并成功写入令牌后，应用才在后台上传游客记录。
- 登录同步失败不会阻断登录和学习；后续再次主动登录会按相同记录键幂等重试。
- 旧版本本机记录首次同步时自动补齐客户端修改时间；新产生的本机记录每次保存都会更新时间。
- 单次最多上传 100 条，客户端会自动分批；空本机记录也会向服务端拉取账号已有快照。
- 另一设备登录后会收到该账号同产品的最新快照；仅当服务端记录更新时才覆盖本机对应内容版本。
- 服务端冲突键为“用户＋产品＋记录类型＋内容 ID＋内容版本”，同一键按客户端修改时间选择较新快照。

#### 数据与接口

- 新增 `user_guest_learning_records` 表，保存产品、记录类型、内容 ID、内容版本、来源设备、客户端修改时间和 JSON 快照。
- `POST /api/learning/guest-records/merge`：登录用户批量幂等合并，并返回账号当前产品的最新快照。
- `GET /api/learning/guest-records`：登录用户读取当前产品全部游客快照，为后续手动同步入口保留。
- 时政产品只接受 `theory_article_quiz`，申论产品只接受 `shenlun_short_practice`；不允许跨产品写入。
- 每条 JSON 快照限制 50 KB，批次限制 100 条，设备标识限制为安全字符；异常未来时间被拒绝，防止错误时钟长期压制正常记录。
- 此表不写入积分、正式错题、每日任务或个人三刀开采表，保留“游客快照”和“账号正式学习资产”的语义边界。

#### 验证证据

- 游客合并专项测试：4 项通过，覆盖幂等、较新版本胜出、跨设备读取、产品隔离、非法设备标识、超大快照、异常未来时间和无业务副作用。
- 后端全量测试：162 项通过；现有 135 条 JWT UTC 时间弃用警告不影响结果。
- 两个独立应用的 TypeScript 检查均通过。
- 本单元两个同步工具、两个 API 文件、两个学习页面和认证工具的 ESLint 检查均通过。
- 知行日知 H5、知行日知微信小程序、知行策论 H5、知行策论微信小程序四项生产构建全部通过。
- 构建仍只有既有 Sass `@import` 与 legacy JS API 弃用提示。

#### 明确限制

- 当前仅在主动登录或注册时同步，没有常驻后台轮询，也没有“立即同步”按钮和可见同步状态。
- 冲突处理依赖客户端修改时间；服务端已拦截明显未来时间，但没有逐字段合并，同一内容版本以整条较新快照为准。
- 登录同步保存的是游客练习快照，不会自动转成积分、正式错题、学习天数或个人三刀开采记录。
- 用户在同一登录会话中继续修改游客页面时，目前仍只更新本机；需下次主动登录才上传，后续可增加明确的手动同步或保存即同步。
- 尚未部署到云服务器进行真实账号、跨浏览器和微信真机联调。

#### 下一开发单元

`U04：时政分辑编排、长期重点专题和固定入口后台`。

目标是在共用管理后台维护每日内容、五题分辑、全文合集开放状态和“十五五规划”等长期重点专题，并让前台只展示已审核发布的入口；不开发整套试卷。完成后按同样规程验证、归档、提交并推送。

### U04：时政分辑编排、长期重点专题和固定入口后台

- 状态：本地开发、验证、归档、提交与 GitHub 推送均已完成。
- 完成日期：2026-09-07。
- 功能提交：`4d9a670 feat(theory): add curated learning entries`。
- 产品范围：共用管理后台、知行日知 H5 与微信小程序、共用 FastAPI 后端。

#### 实际交付

- 管理后台新增“时政学习入口”，将“文章已发布”和“学习入口已开放”拆成独立审核门槛。
- 管理员可选择已发布文章，设置入口标题与说明、今日内容、长期重点、展示日期、排序、草稿/发布状态和全文合集开关。
- 题目可按审核顺序一键填充，也可通过题目 ID 顺序人工调整；保存时每 5 题自动形成一个分辑，最后一辑允许不足 5 题。
- 发布时校验题目必须属于该文章、状态已通过、证据与解析完整、选项和答案有效；题目重复或单辑超过 5 题会被拒绝。
- “全文合集”只有在该文章全部有效题目均审核完整且全部编入分辑时才能开放。
- 前台“选择学习内容”按今日内容与长期重点展示入口，标明题数、来源和合集状态。
- 前台“长期学习”页优先展示长期重点文章，同时保留审核期可用的学习方法卡片。
- 文章、题目或入口日期后来失效时，公开服务按失败关闭原则自动隐藏该入口。
- 删除文章时同步删除其入口编排，避免外键阻断和孤立配置。

#### 数据与接口

- 新增 `theory_learning_entries` 表：一篇文章对应一条入口编排，保存今日/长期重点标记、分辑 JSON、合集开关、展示窗口、排序和发布状态。
- `GET /api/learning/entries`：匿名返回当前时政产品有效且通过实时完整性校验的入口。
- 原有 `GET /api/learning/articles/{article_id}` 与判题接口现在必须先通过入口发布校验，并严格使用后台配置的分辑与题目顺序。
- `GET /admin/theory-learning/entries`：管理端查看全部编排及当前校验错误。
- `PUT /admin/theory-learning/entries/{article_id}`：创建或更新单篇文章学习入口。
- 编排对象是单篇文章学习内容，不创建 `ExamPaper`、统一套卷或整卷作答记录。

#### 验证证据

- 时政入口及匿名学习相关测试：22 项通过，覆盖五题分辑、合集完整性、草稿隔离、展示日期、长期重点、跨产品隔离、题目撤审和版本变化。
- 后端全量测试：171 项通过；既有 135 条 JWT UTC 时间弃用警告不影响结果。
- 时政应用 TypeScript 与本单元相关 ESLint 检查通过。
- 管理后台 `vue-tsc` 与生产构建通过；仅有既有第三方 PURE 注释和大包体提示。
- 知行日知 H5 与微信小程序生产构建均通过；仍只有既有 Sass 弃用提示。

#### 明确限制

- 数据库升级后不会自动把历史文章开放为学习入口；需要管理员逐篇编排并发布，符合“逐步开放”的产品策略。
- 后台当前按题目 ID 文本顺序编排并提供自动填充，尚未实现拖拽排序、题干预览或跨分辑拖动。
- 今日入口目前由运营手动标记与日期窗口控制，尚未根据每日内容运营排期自动生成。
- 长期重点支持持续展示，但还没有独立专题封面、专题层级和专题内多文章排序页面。
- 尚未部署到云服务器验证管理后台真实数据与 H5/小程序联动。

#### 下一开发单元

`U05：内容发布包、渠道入口和发布前检查`。

目标是围绕公众号、小红书、知乎、微信视频号等渠道，从同一已审核母内容生成可连续复用的渠道草稿，统一关联 H5 深链与小程序页面参数，并在导出前检查标题、正文、入口、审核状态和敏感表述；本单元只生成草稿与物料清单，不自动向外部平台发布。完成后按同样规程验证、归档、提交并推送。

### D01：杜衡阁公众号运营回复与双小程序增长最终方案

- 状态：七份备选方案已完成横向取舍，最终设计已沉淀；本单元仅制定方案，未配置公众号或编码。
- 完成日期：2026-09-07。
- 方案文档：`docs/公众号回复设计方案/杜衡阁公众号运营回复与双小程序增长最终方案.md`。

#### 核心决策

- 杜衡阁负责长文解释、品牌信任、内容沉淀和双产品分流；完整阅读、答题、反馈与学习记录留在知行日知和知行策论，不在公众号聊天中重复开发。
- 新关注回复只推动“今日”一个首次动作；一级意图保持今日、时政、申论、菜单，未开放的“我的”不展示。
- 每个运营内容包只设置一个主任务，通过结构化产品、内容、入口、渠道和 campaign 参数精确进入对应学习单元。
- 保留渠道归因、长期重点补位和真实学习结果分享；放弃强制裂变、虚构数据、多条刷屏欢迎、未经权限验证的主动召回和公众号内长流程答题。
- 公众号正式接入后置到两个小程序具有稳定正式入口之后，当前继续实施 U05。

### U05：内容发布包、渠道入口和发布前检查

- 状态：本地开发、验证、提交与 GitHub 推送均已完成。
- 完成日期：2026-09-08。
- 功能提交：`6a63a6c feat(content-ops): validate channel publishing entries`。
- 产品范围：共用 FastAPI 后端与管理后台；未修改公众号配置，未自动发布外部平台。

#### 实际交付

- 复用既有账号运营模块，为发布包增加结构化学习入口：内容时效类型、入口 ID、H5 路径、小程序路径、小程序码场景值和公众号承接词。
- 管理后台只能从当前已审核且公开可访问的知行日知／知行策论入口中选择承接目标，不再依赖手填自由文本链接。
- 知行日知入口实时复用分辑编排的发布、日期和题目完整性校验；知行策论入口实时要求文章和完整三刀教研示范均已发布。
- 当前默认渠道调整为公众号、小红书、知乎和微信视频号；抖音、B站旧发布包仍保持兼容，但不作为首发默认渠道。
- 各渠道草稿自动获得 `channel/campaign/content/product/entry` 归因参数，同一发布包保持一个同主题学习落点。
- 新增发布前检查页面，逐项展示活动标识、排期、母内容、双端路径、场景值、公众号承接词、渠道正文、运营表述、归因入口和时效类型。
- 发布前检查未通过时，后端会阻止“运营通过→待发布”、导出和确认已发布；已通过的发布包如果后来发生内容撤审或入口过期，也会立即失败关闭。
- 导出协议升级为 `content-publish-package/v2`，同时包含结构化入口、检查结果、各渠道人工发布清单和最终核对项。
- 对外运营称呼统一为“时政学习／申论学习”，并兼容迁移旧公众号模板中的“政治理论任务”槽位。

#### 数据与接口

- `content_publish_packages` 新增 `entry_target_json`；旧 SQLite 数据库由幂等兼容补列自动升级。
- `GET /admin/content-ops/entry-targets?productKey=...`：返回当前可公开推广的同产品学习入口和系统建议路径。
- `GET /admin/content-ops/packages/{id}/preflight`：返回结构化检查项、阻断数量和已解析入口。
- 创建、编辑和从文章生成发布包均支持 `entryTarget`；服务端统一重建渠道归因链接，客户端不能用手工链接绕过。

#### 验证证据

- 后端全量测试：171 项通过；覆盖入口选择、双产品隔离、四渠道、结构化归因、检查通过、入口失效阻止待发布／导出和 v2 导出协议。
- 管理后台 `vue-tsc` 与生产构建通过；仅保留既有第三方 PURE 注释和大包体提示。
- 知行日知 H5、微信小程序生产构建通过。
- 知行策论 H5、微信小程序生产构建通过；H5 与小程序同时构建会争用同一 `dist`，验证时已改为顺序执行，发布命令也必须顺序构建同一应用的不同目标。
- 所有目标文件 `git diff --check` 通过。

#### 明确限制

- 当前生成的是平台草稿、路径和场景值清单，不调用公众号、小红书、知乎或视频号接口，不生成正式小程序码图片。
- H5 路径使用部署内相对路径；正式域名、部署前缀和 HTTPS 需要在 U06 生产环境验证。
- 小程序路径已对应当前页面，但 AppID、线上版本和公众号实际关联能力尚未真机核验。
- 发布前阻断词是确定性首发清单，不能替代平台审核和人工教研；原文中的真实正式名称不应因运营检查被篡改。
- 管理后台当前只记录发布包整体“已发布”，尚未逐渠道回填实际 URL、发布时间和失败原因。
- 公众号最终回复方案仍只作为设计依据，待两个产品正式入口稳定后再实施。

#### 下一开发单元

`U06：H5 部署、小程序真机联调与提审材料收口`。

先完成可重复的一键部署与发布前检查，核验服务器环境、域名／备案阻塞、两个 H5 路由和 API，再整理两个小程序可直接导入微信开发者工具的构建产物、环境配置、隐私与提审清单。涉及真实服务器写入、平台登录、域名配置或提交审核时，仍需使用已授权凭据和实际平台能力。

## 3. 待后续排期

- U06-B：云服务器部署、正式域名／公网 IP 路由验证、小程序真机联调与提审材料收口。

单元顺序可依据真实内容准备情况调整，但每次调整需在归档中记录原因。

### U06-A：双产品发布工具链与本地发布产物

- 状态：本地开发、验证、提交与 GitHub 推送均已完成；云服务器部署与微信真机操作留在 U06-B。
- 完成日期：2026-09-08。
- 功能提交：`2e15337 feat(release): harden dual-product deployment`。
- 产品范围：共用后端、管理后台、知行日知 H5／微信小程序、知行策论 H5／微信小程序的发布工具链。

#### 实际交付

- 一键部署现在同时验证综合 H5、知行日知、知行策论、学员 API、管理后台和健康检查，不再只检查综合首页。
- 部署前阻止示例 `SECRET_KEY` 和管理员密码进入生产运行；检查过程不显示密钥内容。
- 服务器更新脚本删除 `git reset --hard` 回退：存在 tracked 修改、detached HEAD 或分支无法快进时直接停止并要求人工处理，避免覆盖服务器文件。
- 新增双产品发布构建脚本，严格顺序构建每个应用的 H5 与微信小程序，并在下一目标覆盖 `dist` 前复制到独立目录。
- 微信小程序归档同时包含 `project.config.json` 与 `dist`，可以按产品独立导入微信开发者工具。
- 新增只读发布前检查，验证登记 AppID、小程序编译目录、生产环境关键配置、四类产物和五个线上路由。
- 部署文档统一使用“知行日知／知行策论”，明确公网 IP 回调联调与正式 HTTPS 小程序域名的边界。

#### 验证证据

- 三个 Shell 脚本均通过 `bash -n`；Python 检查脚本通过编译检查。
- 源码预检 4 项通过、0 阻断；两个 `urlCheck=false` 作为正式真机验收警告保留。
- 在 `/private/tmp/zhixing-release-u06-20260908-retry` 实际顺序生成知行日知／知行策论的 H5 与微信小程序共四份产物。
- 产物预检 9 项通过、0 阻断；四份产物和 `RELEASE.txt` 均完整。
- 后端全量测试 171 项通过；143 条警告仍来自 JWT 依赖的 UTC 弃用提示。
- 四次前端构建通过；既有 Sass `@import` 和 legacy JS API 弃用提示不影响产物。

#### 明确限制

- 本单元使用 `https://zhixinggk.ltd` 作为临时构建验证地址，不代表该域名当前已完成备案、证书和微信合法域名配置。
- 尚未连接用户刚重置的云服务器，也未写入服务器 `.env`、安全组、Nginx 或 Docker 环境。
- 尚未使用微信开发者工具完成真机、体验版和提审操作。
- 项目配置保留 `urlCheck=false` 以便本地联调；正式验收必须打开域名校验并使用微信后台登记的 HTTPS 域名。

#### 下一开发单元

`W01：公众号服务器回调基础能力`。

用户重置云服务器期间先在本地实现 URL 验证、消息签名校验、关注／文本消息解析、确定性基础回复、重复消息保护和安全配置；服务器就绪后再将 W01 与 U06-B 一起部署验证。

### W01：杜衡阁公众号服务器回调基础能力

- 状态：本地开发、验证、提交与 GitHub 推送均已完成；等待云服务器完成重置后部署联调。
- 完成日期：2026-09-08。
- 功能提交：`40061d9 feat(wechat): add official account callback foundation`。
- 接入范围：个人主体、未认证公众号的基础服务器 URL 验证、关注事件与文字被动回复；不预设高级接口权限。

#### 实际交付

- 新增 `GET /api/wechat/callback`，使用公众号 Token、时间戳与 nonce 校验 SHA-1 签名，通过后原样返回 `echostr`。
- 新增同路径 POST 回调，接收微信明文 XML，解析关注、取消关注、文字消息、其他事件和非文字消息。
- 首发确定性意图为“今日、时政、申论、菜单”，同时兼容已确认的少量自然语言别名；未知输入进入短兜底。
- 关注回复保持一屏内说明价值和唯一首次动作，不在公众号内模拟完整答题流程。
- 时政与申论回复分别进入知行日知、知行策论；公网基地址由服务器环境变量配置，非法或缺失地址不会拼出错误链接。
- 默认关闭公众号回调；只有显式启用且 Token 不少于 16 位时才接收微信验证和消息。
- XML 正文限制为 64 KB，并拒绝 DOCTYPE/ENTITY；无效签名、残缺消息和异常 XML 失败关闭。
- 使用消息 ID 或事件指纹进行五分钟进程内去重，限制最多 2048 条，防止微信短时重试重复执行同一路由。
- 当前不支持的 AES 安全模式会明确返回 501，不会把密文误当普通消息处理。
- 发布前检查新增公众号 Token 与公开入口配置检查，检查结果不输出密钥值。

#### 服务器环境变量

```dotenv
WECHAT_OFFICIAL_ENABLED=true
WECHAT_OFFICIAL_TOKEN=16～32位英文或数字且与公众号后台一致
WECHAT_OFFICIAL_APP_ID=杜衡阁公众号AppID
WECHAT_OFFICIAL_PUBLIC_BASE_URL=http://公网IP
```

真实值只写服务器 `.env`，不得进入仓库、日志或归档。公网 IP 明文回调用于技术联调；备案与证书完成后统一切换到 HTTPS 域名。

#### 验证证据

- 公众号专项测试 5 项通过，覆盖默认关闭、URL 验证、错误签名、关注回复、时政链接、兜底、非文字消息、取消关注、异常 XML 和 AES 拒绝。
- 后端全量测试 176 项通过；143 条警告仍来自 JWT 依赖的 UTC 弃用提示。
- 所有变更文件通过 Python 编译检查与 `git diff --check`。

#### 明确限制

- 当前只支持公众号后台选择“明文模式”；正式安全模式的 AES 解密、签名与加密回复尚未实现。
- 去重缓存在单进程内存中，符合当前单容器单 worker 部署；扩展多 worker 或多实例前应迁移为共享存储。
- “今日”暂时指向知行日知首页，不会自动选择未经运营审核的当天内容；后续通过结构化入口绑定替换。
- 当前不调用 `access_token`、自定义菜单、客服消息、素材、小程序码或用户信息接口，不受这些高级权限的实现假设影响。
- 尚未用杜衡阁公众号真实 AppID/Token 完成微信服务器验证。

#### 下一开发单元

`U06-B：云服务器部署与公众号明文回调联调`。

服务器完成重置后部署当前分支，配置随机 Token 和公网入口，依次验证双 H5、后端、管理后台、微信 URL 验证、关注与四类关键词；如果服务器信息尚未就绪，则继续本地开发 R1 统一入口解析与渠道事件。

### U06-B1：独立云服务器部署模式调整

- 状态：脚本调整、验证、提交与 GitHub 推送均已完成；等待连接真实服务器执行部署。
- 完成日期：2026-09-08。
- 功能提交：`f478faf feat(deploy): default standalone server to port 80`。

#### 调整原因与结果

- 原部署默认值为 `127.0.0.1:8081`，用于与同机其他项目共用宿主机 Nginx；当前云服务器重置后只部署本项目，不再需要为另一后端预留公网 80。
- 备案前默认调整为 `HTTP_BIND=0.0.0.0`、`HTTP_PORT=80`，项目容器直接承载综合 H5、两个垂直 H5、后端、管理后台和公众号回调。
- 备案与证书完成后仍保留第二阶段方案：容器改回 `127.0.0.1:8081`，宿主机 Nginx 监听 80/443、管理 HTTPS 证书并反向代理。
- Docker Compose 默认值、`.env` 示例、一键部署输出、Docker 初始化注释、Nginx 模板和部署文档已统一，避免文档与实际监听方式冲突。
- 首次生成 `.env` 时自动生成 32 位十六进制公众号 Token，默认不启用公众号回调，也不在部署日志打印 Token。
- 一键部署会校验端口范围与绑定地址；公网直连时输出 H5、管理后台和公众号回调入口，并提示 HTTP 阶段不要传输管理端敏感信息。

#### 验证证据

- 部署、Docker 初始化、更新与发布构建脚本均通过 `bash -n`。
- 发布前源码检查 4 项通过、0 阻断；两个 `urlCheck=false` 警告留待微信正式合法域名验收。
- 公众号回调专项测试 5 项通过。
- 所有目标文件通过 `git diff --check`。

#### 明确限制

- 公网 80 是否可用仍取决于服务器系统服务和云安全组；真实部署时必须检查是否有系统 Nginx/Apache 占用端口。
- 直接 HTTP 仅用于备案前 H5 浏览和公众号明文回调联调，不用于小程序正式 API，也不建议登录管理后台。
- 当前没有自动修改云厂商安全组、DNS、备案、证书或微信公众平台配置。

#### 下一步

服务器就绪后执行 U06-B2：连接服务器、切换到当前功能分支、生成生产 `.env`、开放 80、部署容器、完成路由预检，然后由用户在公众号后台配置同一回调 URL 与 Token。

### U06-B2：独立云服务器部署与公众号回调联调

- 状态：服务器部署、公网路由和公众号明文回调技术联调已完成；等待用户在微信公众平台保存并启用新配置。
- 完成日期：2026-09-08。
- 部署分支：`feat/qb-pipeline`，服务器部署基线 `a3ee26c`。
- 公网入口：`http://124.223.178.64`；备案和证书完成前仅用于 H5 预览及公众号明文回调联调。

#### 实际部署结构

- 服务器为 Ubuntu 22.04，Docker 与 Docker Compose 可用；系统 Nginx 继续占用公网 80 端口。
- 为避免破坏服务器存量项目，知行容器仅绑定 `127.0.0.1:8081`，由宿主机 Nginx 统一反向代理公网请求。
- 原 `coffee-order` 站点仅停用 Nginx 入口；其站点文件、项目目录、容器和数据均未删除，可手动回退。
- 生产 `.env` 已限制为 600 权限，并备份部署前版本；JWT 密钥、管理员密码和公众号 Token 均已轮换，真实值未进入日志或仓库。
- 公众号基础回调已启用，公开基地址指向当前公网 IP；公众号 AppID 暂未填写，不影响明文模式的 URL 验证与被动回复。
- Docker 命名卷 `zhixing-gongkao_zhixing-gongkao-data` 被继续挂载至 `/app/server/data`，原 SQLite 数据库及 WAL 文件保留，未初始化或删除旧数据。

#### 验证证据

- 容器状态为 `healthy`，`/health` 返回正常服务标识。
- 公网 `/`、`/theory/`、`/shenlun/`、`/manage/`、`/health`、`/api/config` 全部返回 HTTP 200。
- 公众号回调公网路径 `/api/wechat/callback` 可达；缺少参数时返回 422，说明请求已进入正确 FastAPI 路由。
- 使用服务器 `.env` 中 Token 在服务器内部计算微信 SHA-1 签名，GET 验证成功并正确原样返回 `echostr`。
- 伪造签名返回 HTTP 403，证明签名校验生效；整个验证过程未输出 Token。
- Nginx 最终配置通过 `nginx -t`，重载后服务状态为 `active`。

#### 微信公众平台待办

- 将消息推送 URL 改为 `http://124.223.178.64/api/wechat/callback`。
- Token 必须使用本次服务器新生成的值；由用户登录服务器本地查看并粘贴到公众平台，不通过聊天、Git 或文档传递。
- 当前选择明文模式、XML 格式；旧 EncodingAESKey 应在微信后台重新生成并妥善保存，但本阶段明文回调不会使用它。
- 保存并通过服务器验证后启用消息推送，再用真实微信完成关注、`今日`、`时政`、`申论`、`菜单` 与未知文本验收。
- 当前 API IP 白名单已包含新服务器 IP；确认旧服务永久下线后再从白名单移除旧 IP。

#### 明确限制

- HTTP 不满足微信小程序正式 `request` 合法域名要求；备案、DNS 和 HTTPS 证书完成后，必须切换正式域名并更新 H5、公众号和小程序配置。
- 管理后台当前可通过 HTTP 打开，但不应在公网 HTTP 环境输入管理员密码；正式使用前需启用 HTTPS，并建议增加访问限制。
- 公众号后台保存、启用及微信真机消息验收需要用户登录微信公众平台完成，当前仅完成服务器侧技术验证。

#### 下一开发单元

完成微信后台人工配置的同时，进入 `R1：统一内容入口解析与渠道归因`，让公众号关键词、运营帖子和小程序码能够落到同一篇内容、同一组练习，并记录来源平台与活动标识。

### U06-B3：公众号 Token 格式修正

- 状态：已修正并重新完成服务器侧签名验证。
- 完成日期：2026-09-08。
- 修正原因：首次部署脚本生成了 48 位十六进制 Token，超过微信公众平台 32 位上限，导致后台输入时直接报格式错误。
- 修正结果：首次部署统一生成 32 位十六进制随机 Token；部署检查、发布前检查和回调运行时均要求 16～32 位英文或数字，提前拦截微信后台无法接受的值。
- 服务器 `.env` 已轮换为新的合规 Token，容器重建后正确签名通过、伪造签名返回 403；新 Token 未输出到日志或仓库。

### W01-B：数字导航与回复视觉层级优化

- 状态：开发、全量验证、提交、推送及服务器部署均已完成。
- 完成日期：2026-09-08。
- 功能提交：`2b74d98 feat(wechat): add elegant numeric navigation`。

#### 实际交付

- 新增数字入口：回复 `1` 进入今日学习，`2` 进入时政学习，`3` 进入申论学习，`0` 返回学习导航；原中文关键词继续兼容。
- 关注欢迎语直接展示数字菜单，降低手机输入成本，不要求用户记忆或切换中文输入法。
- 所有回复统一为“杜衡阁标题—任务或学习路径—独立链接—返回导航”的四层结构，使用留白和短句控制阅读节奏，不堆叠表情符号与装饰线。
- 今日学习明确为“阅读重点内容，再完成一组 5 题练习”；时政与申论分别展示自己的完整学习路径。
- 未识别消息、非文字消息和普通事件均引导回复 `0`，形成随时可返回的稳定导航闭环。

#### 二维码与微信内导航决策

- 微信聊天内不把二维码作为主导航：用户通常正在同一台手机上阅读消息，二维码需要截图或长按识别，步骤多于直接点击链接。
- 当前不生成指向公网 IP 的二维码，避免备案后域名切换导致已发布二维码永久失效，也避免 HTTP 安全提示影响信任。
- 小程序提审上线后，微信内优先升级为小程序卡片或可直达小程序的内容入口；公众号文章内使用小程序组件。
- 公众号文章配图、线下物料、小红书、知乎和视频号封面／结尾页使用带渠道参数的小程序码，承担跨平台导流。
- 如后续需要用户回复“二维码”获取图片，可在配置公众号 AppID/AppSecret 后上传永久素材，保存 `media_id` 并返回图片消息；不把二维码二进制直接塞入文字回复。

#### 验证证据

- 公众号专项测试 7 项通过，新增 `0/1/2/3` 路由及新版文案断言。
- 后端全量测试 178 项通过；143 条警告仍来自 JWT 依赖的 UTC 弃用提示。
- Python 编译检查与 `git diff --check` 通过。
- 生产服务器完成容器重建，综合 H5、两个垂直 H5、API、管理后台与健康检查均通过。

#### 下一开发单元

进入 `R1：统一内容入口解析与渠道归因`。首先让“今日学习”从固定首页链接升级为运营审核后的当天内容入口，再为公众号文章和外部平台生成稳定渠道参数，为后续小程序码做准备。

### U06-B4：双 H5 底部栏图标修复

- 状态：排查、修复、提交、推送、生产部署与移动端视觉验收均已完成。
- 完成日期：2026-09-08。
- 图标提交：`2be21ec fix(vertical-apps): redraw tabbar icons`。
- 路径提交：`46d7b8c fix(vertical-apps): scope h5 tabbar asset paths`。

#### 根因与修复

- 第一层问题是旧 PNG 使用 `81×81` 画布，但有效图形仅约 `44～52px`；Taro H5 固定缩放到 27px 后，实际图形只剩约 `13～17px`，视觉上过小且不协调。
- 第二层问题是 Taro H5 将原生 TabBar 图片生成为根路径 `/static/images/*`，而两个独立应用部署在 `/theory/` 与 `/shenlun/`，导致未选中图标请求到错误资源并显示为破图。
- 重新绘制今日、阅读、练习、我的四组统一线性 PNG：未选中使用中性灰，选中使用中国红；保持 `81×81` 小程序兼容画布，将有效图形扩大到约 `60～73px`。
- 两应用新增构建时 `APP_PUBLIC_PATH`，H5 `onShow` 后通过标准 TabBar API 同步每项图标为应用自己的静态资源路径；微信小程序仍使用原生相对路径，不受 H5 子路径影响。

#### 验证证据

- 两应用共 16 个 PNG 全部通过尺寸、透明通道和有效图形范围检查；同名图标跨应用二进制一致。
- 两套 TypeScript 配置均通过 `tsc --noEmit`。
- Docker 生产构建中，知行日知与知行策论 H5 均成功生成 8 个新图标资源；既有 Sass 弃用提示不影响构建。
- 公网移动端 DOM 验证显示：时政学习 4 项全部使用 `/theory/static/images/*`，申论学习 4 项全部使用 `/shenlun/static/images/*`；每张图片均 `complete=true`、自然尺寸 `81×81`。
- 390×844 移动端截图复验：四个图标均正常显示，选中项为中国红，未选中项为灰色，无破图和错位。
- 容器重新部署后健康检查、综合 H5、两个垂直 H5、学员 API 与管理后台均通过。
