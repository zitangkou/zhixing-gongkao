# 01 · 迁移清单（按文件级）

> 源仓库：`zhengkao-tong`（知行，冻结）
> 目标仓库：`zhixing-gongkao`（知行公考，v1）
> 策略：复制改造，不做代码级共享
> 验收：执行时按本文档逐行打勾，零遗漏

---

## 0. 复制基线

```bash
# 在新仓库目录下
cd ~/Projects/zhixing-gongkao
cp -r ../zhengkao-tong/src/ .
cp -r ../zhengkao-tong/server/ .
cp ../zhengkao-tong/{package.json,package-lock.json,babel.config.js,tsconfig.json,Dockerfile,docker-compose.yml,.dockerignore,.eslintrc.js,.prettierrc,.npmrc,project.config.json,config,scripts,docker,types} .
cp -r ../zhengkao-tong/obsidian/ .  # 知识框架 md 样例
# README、DEPLOY、ARCHITECTURE 全部重写，不复制
```

**注意**：
- `.env.docker.example` 复制后改名 `.env.docker`，并修改应用名/端口
- `data/` 目录（数据库、uploads）**不复制**——新库从空开始
- `node_modules/`、`.venv/`、`dist/` 不复制（依赖重装）

---

## 1. 保留清单（14 域 56 页）

> ✅ = 整段保留，仅做"知识框架"（KnowledgeNode 双向跳转）等小增强
> 🔧 = 保留主体，需做小幅改造（标注改造点）

### 1.1 认证域（auth）✅

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 登录页 | `src/pages/auth/login.vue` | 保留 | 仅替换品牌色、文案 |
| 注册页 | `src/pages/auth/register.vue` | 保留 | 同上 |
| 登录注册接口 | `server/app/api/public/auth.py` | 保留 | 无 |
| 用户模型 | `server/app/models/__init__.py:AppUser` | 保留 | 无 |
| 鉴权中间件 | `server/app/core/security.py` | 保留 | 无 |
| 前端鉴权工具 | `src/utils/auth.ts` | 保留 | 无 |

### 1.2 用户域（user）🔧

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 我的页（Tab） | `src/pages/user/index.vue` | 保留 | 去掉充值入口、改成"上岸"风格 |
| 个人资料 | `src/pages/user/profile.vue` | 保留 | 无 |
| 签到 | `src/pages/user/signin.vue` | 保留 | 文案"知行"→"知行公考" |
| 积分流水 | `src/pages/user/points.vue` | 保留 | 无 |
| 排行榜 | `src/pages/user/rank.vue` | 保留 | 无 |
| 成长足迹 | `src/pages/user/growth.vue` | 保留 | 升级为 M4 能力雷达数据源 |
| 反馈 | `src/pages/user/feedback.vue` | 保留 | 无 |
| **删除** | `src/pages/user/recharge.vue` | ❌ 不复制 | v1 无充值 |
| 后端用户服务 | `server/app/services/user_service.py` | 保留 | 无 |
| 成长聚合 | `server/app/services/growth_service.py` | 保留 | M4 接入雷达图 |

### 1.3 时政文章域（article）🔧

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 文章详情 | `src/pages/article/detail.vue` | 保留 | M2 增强：文章↔知识框架双向关联 |
| 思维导图 | `src/pages/article/mindmap.vue` | 保留 | 无 |
| 文章服务 | `server/app/services/article_service.py` | 保留 | M2 增强：返回 knowledge_node_ids |
| 学习服务 | `server/app/services/study_service.py` | 保留 | 写入 `activity_event.article_read` |
| 推荐文章 | `server/app/services/featured_article.py` | 保留 | 无 |
| SectionRead 模型 | `server/app/models/__init__.py:SectionRead` | 保留 | 无 |

### 1.4 刷题练习域（question）🔧

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 刷题首页 | `src/pages/question/index.vue` | 保留 | M4 接入"今日精选"（按薄弱点） |
| 答题页 | `src/pages/question/taking.vue` | 保留 | 极简 + 即时反馈 |
| 文章选题 | `src/pages/question/article-pick.vue` | 保留 | 无 |
| 错题重练 | `src/pages/question/review.vue` | 保留 | 无 |
| 错题列表 | `src/pages/question/wrong.vue` | 保留 | 无 |
| 手动错题列表 | `src/pages/question/manual-list.vue` | 保留 | M2 增强：错题↔知识框架跳转 |
| 手动错题答题 | `src/pages/question/manual-quiz.vue` | 保留 | 无 |
| 手动错题录入 | `src/pages/question/manual-edit.vue` | 保留 | 无 |
| 刷题服务 | `server/app/services/quiz_service.py` | 保留 | 写入 `activity_event.quiz_done` |
| 题目服务 | `server/app/services/question_service.py` | 保留 | 无 |
| 错题服务 | `server/app/services/wrong_service.py` | 保留 | 写入 SRS |
| 手动错题服务 | `server/app/services/manual_wrong_service.py` | 保留 | 无 |
| WrongAnswer / ManualWrong / QuizAttempt | `server/app/models/__init__.py` | 保留 | 无 |

### 1.5 真题套卷域（exam）✅

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 试卷列表 | `src/pages/exam/list.vue` | 保留 | 无 |
| 试卷详情 | `src/pages/exam/detail.vue` | 保留 | 无 |
| 开考答题 | `src/pages/exam/taking.vue` | 保留 | 交卷时写 `activity_event.exam_done` |
| 成绩报告 | `src/pages/exam/result.vue` | 保留 | M4 接入"成绩卡"生成 |
| 套卷服务 | `server/app/services/exam_service.py` | 保留 | 无 |
| 试卷导入 | `server/app/services/exam_import.py` | 保留 | 无 |
| ExamPaper / ExamQuestion / ExamAttempt / ExamAnswer | 模型 | 保留 | 无 |

### 1.6 资料分析域（ziliao）🔧【**模板模块**】

> 资料分析是 v1 唯一"已成熟"的行测模块（9 页），将作为**模板**，复制经验到言语/数量/判断（v1.5+）。

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 资料分析首页 | `src/pages/ziliao/index.vue` | 保留 | 升级为"行测学习地图"通用壳（M5 复制） |
| 公式库 | `src/pages/ziliao/formulas.vue` | 保留 | KaTeX 渲染 |
| 公式详情 | `src/pages/ziliao/formula-detail.vue` | 保留 | 无 |
| 题型库 | `src/pages/ziliao/types.vue` | 保留 | 无 |
| 题型详情 | `src/pages/ziliao/type-detail.vue` | 保留 | 无 |
| 技巧库 | `src/pages/ziliao/tricks.vue` | 保留 | 无 |
| 技巧详情 | `src/pages/ziliao/trick-detail.vue` | 保留 | 无 |
| 专项练习 | `src/pages/ziliao/drill.vue` | 保留 | 写 `activity_event.drill_done` |
| 练习结果 | `src/pages/ziliao/result.vue` | 保留 | M4 接入"薄弱题型 Top3" |
| 资料服务 | `server/app/services/ziliao_service.py` | 保留 | 无 |
| 资料模型 | `ZiliaoFormula/ZiliaoQuestionType/ZiliaoTrick/ZiliaoPracticeLog` | 保留 | 无 |

### 1.7 知识框架域（knowledge）🔧

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 知识树页 | `src/pages/knowledge/index.vue` | 保留 | M2 增强：节点 ↔ 文章/错题双向跳转 |
| 知识服务 | `server/app/services/knowledge_service.py` | 保留 | 返回 associated_article_ids / wrong_ids |
| 复习服务 | `server/app/services/knowledge_review_service.py` | 保留 | 写入 SRS |
| KnowledgeNode 模型 | 保留 | 保留 | 加字段 `associated_articles JSON`（迁移脚本） |
| Obsidian md 样例 | `obsidian/行测错题`、`obsidian/知识框架` 等 | **选择性复制** | 仅保留公考相关内容 |

### 1.8 复习中心域（review）✅

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 复习聚合 | `src/pages/review/hub.vue` | 保留 | M3 升级：聚合错题+知识点+规范词 |
| 复习答题 | `src/pages/review/quiz.vue` | 保留 | 无 |
| 复习服务 | `server/app/services/review_hub_service.py` | 保留 | 无 |
| SRS 算法 | `server/app/services/srs.py` | 保留 | 无 |

### 1.9 申论·人民日报域（rmrb）✅

> 差异化重点，整段保留。

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 申论首页 | `src/pages/rmrb/index.vue` | 保留 | 无 |
| 时评列表 | `src/pages/rmrb/article-list.vue` | 保留 | 无 |
| 时评详情 | `src/pages/rmrb/article-detail.vue` | 保留 | 三刀解剖 |
| 开采本 | `src/pages/rmrb/mines.vue` | 保留 | 无 |
| 开采录入 | `src/pages/rmrb/mine-edit.vue` | 保留 | 写 `activity_event.shenlun_mined` |
| 规范词库 | `src/pages/rmrb/terms.vue` | 保留 | 无 |
| 阶梯训练 | `src/pages/rmrb/drill.vue` | 保留 | 无 |
| 申论服务 | `server/app/services/shenlun_service.py` | 保留 | 无 |
| 人民日报服务 | `server/app/services/rmrb_service.py` | 保留 | 无 |
| 元数据服务 | `server/app/services/rmrb_meta_service.py` | 保留 | 无 |
| 申论模型 | `RmrbArticle/ShenlunMineLog/ShenlunNormTerm/ShenlunDrillLog/ShenlunTermCategory/ShenlunSkeletonTemplate/ShenlunSentenceType/ShenlunArgumentMethod` | 保留 | 无 |

### 1.10 语料本域（corpus）✅

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 语料列表 | `src/pages/corpus/index.vue` | 保留 | 无 |
| 语料编辑 | `src/pages/corpus/edit.vue` | 保留 | 无 |
| 语料服务 | `server/app/services/corpus_service.py` | 保留 | 无 |
| CorpusItem 模型 | 保留 | 保留 | 无 |

### 1.11 学习计划域（plan）✅

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 今日清单 | `src/pages/plan/today.vue` | 保留 | M3 升级为今日驾驶舱子模块 |
| 每日复盘 | `src/pages/plan/review.vue` | 保留 | 无 |
| 本周计划 | `src/pages/plan/week.vue` | 保留 | 无 |
| 单日详情 | `src/pages/plan/day.vue` | 保留 | 无 |
| 计划服务 | `server/app/services/plan_service.py` | 保留 | 无 |
| PlanTask/PlanTemplate/DailyReview 模型 | 保留 | 保留 | 无 |

### 1.12 事件复盘域（events）✅

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 事件列表 | `src/pages/events/index.vue` | 保留 | 无 |
| 事件编辑 | `src/pages/events/edit.vue` | 保留 | 无 |
| 事件服务 | `server/app/services/event_impression_service.py` | 保留 | 无 |
| EventImpression 模型 | 保留 | 保留 | 无 |

### 1.13 基础能力（横切）✅

| 资产 | 源路径 | 目标路径 | 改造点 |
| --- | --- | --- | --- |
| 前端 API 客户端 | `src/api/index.ts` | 保留 | 无 |
| Pinia 状态（user/settings/article/question/plan/knowledge/manualWrong） | `src/store/*.ts` | 保留 | 无 |
| 公共组件（ArticleCard/QuestionItem/MindMap/KnowledgeTree/PointsBadge/SignCalendar/VoiceInputBtn/LatexBlock/...） | `src/components/*.vue` | 保留 | 无 |
| 工具（auth/media/latex/knowledge/memoryCurve/feedback/feedbackHost/formFlush/...） | `src/utils/*.ts` | 保留 | 无 |
| 后端 core（security/permissions/response） | `server/app/core/*.py` | 保留 | 无 |
| 启动初始化 | `server/app/main.py`、`server/app/seed.py` | 保留 | 去掉已删域的 seed 数据 |
| 时区/上传路径/常量 | `server/app/{timezone,upload_paths,constants}.py` | 保留 | 无 |
| 管理后台（admin-web） | `server/admin-web/**` | 保留 + 改造 | 删 6 个域菜单、改菜单分组（见文档 4 §M1） |
| 测试 | `server/tests/**` | 选择性保留 | 至少保留 `test_api_smoke.py` 冒烟 |

---

## 2. 删除清单（6 域 50 页 + 16 端点域 + 多张表）

> ❌ = 完全不复制；下游依赖（菜单/路由/接口注册/seed）同步删除

### 2.1 英语域（english · 16 页）

**删除文件**：
```
src/pages/english/index.vue
src/pages/english/article-list.vue
src/pages/english/article-detail.vue
src/pages/english/vocab.vue
src/pages/english/speaking-list.vue
src/pages/english/speaking-detail.vue
src/pages/english/grammar.vue
src/pages/english/grammar-detail.vue
src/pages/english/phonetic.vue
src/pages/english/phonetic-detail.vue
src/pages/english/tv/index.vue
src/pages/english/tv/show.vue
src/pages/english/tv/episode.vue
src/pages/english/tv/scene-study.vue
src/pages/english/tv/expression-bank.vue
src/pages/english/tv/weekly.vue
```

**删除后端**：
- `server/app/services/english_service.py`
- `server/app/services/phonetic_service.py`
- `server/app/services/tts_service.py`
- `server/app/services/asr_service.py`
- `server/app/services/tv_english_service.py`
- `server/app/api/public/english.py`（如独立文件）
- 表：`EnglishArticle/UserVocab/UserSpeakingSentence/SpeakingLesson/SpeakingAttempt/GrammarLesson/GrammarProgress/EnglishStudyLog/PhoneticLesson/PhoneticProgress/TvShow/TvEpisode/TvScene/TvDialogueLine/TvExpression/TvStudySession`
- seed 中的 DJ 48 音标数据

**删除管理后台**：`admin-web/src/views/english/**`、菜单 `英语学习`

**删除依赖**：
- `src/pages/index/index.vue` 中所有指向 `pages/english/*` 的入口
- `src/pages/user/index.vue` 中"英语"折叠入口
- 根 store 中若有 `englishStore`（检查后删除）
- `server/app/seed.py` 中相关初始化

**风险点**：
- `src/api/index.ts` 是否有英语相关封装 → 检查后删除
- 是否有组件依赖 `English*` 模型 → 全局搜索 `EnglishArticle` 等

### 2.2 读书域（dushu · 7 页）

**删除文件**：
```
src/pages/dushu/index.vue
src/pages/dushu/shelf.vue
src/pages/dushu/book-detail.vue
src/pages/dushu/today.vue
src/pages/dushu/assets.vue
src/pages/dushu/person-edit.vue
src/pages/dushu/summary-edit.vue
```

**删除后端**：
- `server/app/services/dushu_service.py`
- 表：`DushuBook/DushuDailyLog/DushuPersonCard/DushuBookSummary`
- 管理后台 `读书` 菜单

### 2.3 健康域（health · 7 页）

**删除文件**：
```
src/pages/health/index.vue
src/pages/health/today.vue
src/pages/health/mind.vue
src/pages/health/body.vue
src/pages/health/habits.vue
src/pages/health/review.vue
src/pages/health/phase.vue
src/pages/health/ScoreRow.vue
src/pages/health/WeekDots.vue
```

**删除后端**：
- `server/app/services/health_service.py`
- 表：`HealthUserState/HealthDailyLog`
- 管理后台 `健康` 菜单
- seed 中的 8 周恢复计划

### 2.4 账本域（ledger · 6 页）

**删除文件**：
```
src/pages/ledger/index.vue
src/pages/ledger/expense-edit.vue
src/pages/ledger/loan-edit.vue
src/pages/ledger/loan-detail.vue
src/pages/ledger/repay-edit.vue
src/pages/ledger/person.vue
```

**删除后端**：
- `server/app/services/ledger_service.py`
- 表：`LedgerExpense/LedgerLoan/LedgerRepayment`
- 管理后台 `账本` 菜单

### 2.5 财富域（wealth · 7 页）

**删除文件**：
```
src/pages/wealth/index.vue
src/pages/wealth/overview.vue
src/pages/wealth/snapshot-edit.vue
src/pages/wealth/rules.vue
src/pages/wealth/journal.vue
src/pages/wealth/journal-edit.vue
src/pages/wealth/review.vue
```

**删除后端**：
- `server/app/services/wealth_service.py`
- 表：`WealthSnapshot/WealthPrinciple/WealthJournal`
- 管理后台 `财富` 菜单

### 2.6 充值页（recharge · 1 页）

**删除文件**：
```
src/pages/user/recharge.vue
```

**删除后端**：
- 表：`RechargePackage`
- `server/app/seed.py` 中充值套餐初始化
- 管理后台 `会员/充值` 菜单（如有）

---

## 3. 新建清单（v1 必建 6 页 + 3 张表 + 1 个卡片引擎）

### 3.1 今日驾驶舱页

| 项 | 路径 | 说明 |
| --- | --- | --- |
| 页面 | `src/pages/today/index.vue` | v1 第一 Tab 入口 |
| Tab 注册 | `src/app.config.ts` | 替换原"学习"Tab 位 |
| 组件 | `src/components/TodayCountdown.vue` | 倒计时（读 exam_countdown 表） |
| 组件 | `src/components/TodayTaskList.vue` | 今日任务（从 plan_service 拉取） |
| 组件 | `src/components/TodayReviewAlert.vue` | 到期复习聚合 |
| 组件 | `src/components/TodayYesterdayBar.vue` | 昨日数据条（时长/正确率/连续天数） |
| 组件 | `src/components/TodayCardEntry.vue` | 上岸卡片入口 |

### 3.2 倒计时配置页

| 项 | 路径 | 说明 |
| --- | --- | --- |
| 页面 | `src/pages/today/countdown.vue` | 配置国考/省考日期 |
| API | `POST /api/countdown` / `GET /api/countdown` | 增删改查 |
| Service | `server/app/services/countdown_service.py` | 业务逻辑 |
| 模型 | `server/app/models/__init__.py:ExamCountdown` | 见文档 5 |
| Schema | `server/app/schemas/countdown.py` | 入参出参 |

### 3.3 上岸卡片生成器

| 项 | 路径 | 说明 |
| --- | --- | --- |
| 页面 | `src/pages/today/card.vue` | 5 类卡片选择 + 生成预览 |
| 组件 | `src/components/CardCanvas.vue` | Taro Canvas 绘制统一壳 |
| 模板 | `src/components/cards/DailyCheckinCard.vue` | 今日打卡卡 |
| 模板 | `src/components/cards/WeeklyReportCard.vue` | 周报卡 |
| 模板 | `src/components/cards/MilestoneCard.vue` | 里程碑卡 |
| 模板 | `src/components/cards/ScoreCard.vue` | 成绩卡 |
| 模板 | `src/components/cards/MineCard.vue` | 开采卡（v2 留位） |
| 工具 | `src/utils/cardRenderer.ts` | 统一渲染/导出/分享 |
| API | `POST /api/cards` / `GET /api/cards/mine` | 卡片记录 CRUD |
| Service | `server/app/services/card_service.py` | 业务逻辑 |
| 模型 | `server/app/models/__init__.py:CardRecord` | 见文档 5 |

### 3.4 能力雷达图

| 项 | 路径 | 说明 |
| --- | --- | --- |
| 页面 | `src/pages/today/radar.vue` | 雷达图可视化 |
| 组件 | `src/components/AbilityRadar.vue` | 雷达图（行测五模块 + 申论） |
| API | `GET /api/growth/radar` | 聚合 5 模块 + 申论正确率 |
| Service | 扩展 `server/app/services/growth_service.py` | 增加 `get_radar_data(user_id)` |
| 数据源 | `WrongAnswer/QuizAttempt/ZiliaoPracticeLog` | 各模块正确率 + 题数 + 用时 |

### 3.5 今日精选（v1 末 / v1.5 升）

| 项 | 路径 | 说明 |
| --- | --- | --- |
| 逻辑 | 扩展 `server/app/services/quiz_service.py` | `recommend_today(user_id)` 按错题本 + 各模块正确率抽题 |
| 入口 | `src/pages/question/today.vue` | "今日精选"刷题入口 |
| 触发 | 写 `activity_event.quiz_done` | 推荐依据回写 |

### 3.6 智能组卷（v1.5 灰度）

| 项 | 路径 | 说明 |
| --- | --- | --- |
| 页面 | `src/pages/exam/auto.vue` | 按弱项选模块自动组卷 |
| API | `POST /api/exam/auto-generate` | 接收模块列表 + 题数 |
| Service | 扩展 `server/app/services/exam_service.py` | 抽题 → 临时 ExamPaper |

### 3.7 AI 题目讲解（v1.5 灰度）

| 项 | 路径 | 说明 |
| --- | --- | --- |
| 入口 | `src/pages/question/taking.vue` 解析面板"AI 讲解"按钮 | |
| API | `POST /api/llm/explain` | 调 LLM |
| Service | `server/app/services/llm_service.py`（新建） | 包装 LLM 调用 |
| 配置 | 沿用 `LLM_ENABLED` / `LLM_BASE_URL` / `LLM_MODEL` | 默认关闭 |

### 3.8 申论 AI 预批改（v1.5 灰度）

| 项 | 路径 | 说明 |
| --- | --- | --- |
| 入口 | `src/pages/rmrb/drill.vue` 提交答案后"AI 预批改" | |
| API | `POST /api/llm/shenlun-grade` | 接收作答文本 + 题目 |
| Service | 扩展 `llm_service.py` | OCR + LLM 打分 |
| 输出 | 要点齐全度 / 逻辑层次 / 规范表达 / 格式 四维评分 | |

### 3.9 拍照笔记转知识点（v2）

> v1 不做，仅在文档 4 留位。

### 3.10 社区 UI（v2）

> v1 不做，仅在文档 4 留位。

---

## 4. 路由改造（`src/app.config.ts`）

### 4.1 删除路由（47 个）

```ts
// 从 pages 数组中删除
'pages/english/index',
'pages/english/article-list',
'pages/english/article-detail',
'pages/english/vocab',
'pages/english/speaking-list',
'pages/english/speaking-detail',
'pages/english/grammar',
'pages/english/grammar-detail',
'pages/english/phonetic',
'pages/english/phonetic-detail',
'pages/english/tv/index',
'pages/english/tv/show',
'pages/english/tv/episode',
'pages/english/tv/scene-study',
'pages/english/tv/expression-bank',
'pages/english/tv/weekly',
'pages/dushu/index',
'pages/dushu/shelf',
'pages/dushu/book-detail',
'pages/dushu/today',
'pages/dushu/assets',
'pages/dushu/person-edit',
'pages/dushu/summary-edit',
'pages/health/index',
'pages/health/today',
'pages/health/mind',
'pages/health/body',
'pages/health/habits',
'pages/health/review',
'pages/health/phase',
'pages/ledger/index',
'pages/ledger/expense-edit',
'pages/ledger/loan-edit',
'pages/ledger/loan-detail',
'pages/ledger/repay-edit',
'pages/ledger/person',
'pages/wealth/index',
'pages/wealth/overview',
'pages/wealth/snapshot-edit',
'pages/wealth/rules',
'pages/wealth/journal',
'pages/wealth/journal-edit',
'pages/wealth/review',
'pages/user/recharge',
```

### 4.2 新增路由（v1 必建）

```ts
'pages/today/index',           // 今日驾驶舱
'pages/today/countdown',       // 倒计时配置
'pages/today/card',            // 上岸卡片
'pages/today/radar',           // 能力雷达
'pages/question/today',        // 今日精选（v1 末）
'pages/exam/auto',             // 智能组卷（v1.5）
```

### 4.3 调整 Tab 顺序（3 → 4）

```ts
tabBar: {
  color: '#999999',
  selectedColor: '#1E3A5F',  // 深蓝
  backgroundColor: '#FFFFFF',
  list: [
    {
      pagePath: 'pages/today/index',        // ← 新增第一 Tab
      text: '今日',
      iconPath: './assets/icons/today.png',
      selectedIconPath: './assets/icons/today-active.png',
    },
    {
      pagePath: 'pages/index/index',        // 原"学习" Tab
      text: '学习',
      iconPath: './assets/icons/home.png',
      selectedIconPath: './assets/icons/home-active.png',
    },
    {
      pagePath: 'pages/question/index',     // 练习 Tab 不变
      text: '练习',
      iconPath: './assets/icons/quiz.png',
      selectedIconPath: './assets/icons/quiz-active.png',
    },
    {
      pagePath: 'pages/user/index',         // 我的 Tab 不变
      text: '我的',
      iconPath: './assets/icons/user.png',
      selectedIconPath: './assets/icons/user-active.png',
    },
  ],
}
```

**图标资源**：从 `src/assets/icons/` 中确认 `today.png` / `today-active.png` 存在；M1 任务包含"生成今日 Tab 图标"。

### 4.4 窗口样式改造

```ts
window: {
  navigationBarBackgroundColor: '#1E3A5F',  // 深蓝（替代中国红）
  navigationBarTitleText: '知行公考',         // 替代"知行"
  navigationBarTextStyle: 'white',
  backgroundColor: '#F5F7FA',              // 浅冷灰（替代 #F3F4F6）
}
```

---

## 5. 后端 API 注册改造

### 5.1 删除 API 路由（`server/app/api/public/routes.py`）

```python
# 注释或删除以下路由注册
# router.include_router(english_router)
# router.include_router(dushu_router)
# router.include_router(health_router)
# router.include_router(ledger_router)
# router.include_router(wealth_router)
# router.include_router(tv_english_router)
# router.include_router(phonetic_router)
# router.include_router(tts_router)
# router.include_router(asr_router)
```

### 5.2 新增 API 路由

```python
from app.services.countdown_service import router as countdown_router
from app.services.card_service import router as card_router
from app.services.llm_service import router as llm_router  # v1.5

router.include_router(countdown_router)  # /api/countdown
router.include_router(card_router)        # /api/cards
# router.include_router(llm_router)      # v1.5 默认关闭
```

### 5.3 管理后台菜单改造（`server/admin-web/src/config/nav.ts`）

**删除菜单**：
- 英语学习（articles/lessons/grammar/phonetic/tv 全部）
- 读书管理
- 健康管理
- 账本管理
- 财富管理
- 会员/充值

**重排菜单**（见最终方案 §10）：
- 内容运营（文章/分类）
- 题库建设（题库/试卷）
- 资料库（资料分析/知识框架/人民日报/语料）
- 学习运营（计划模板/推荐位/积分规则）
- 数据洞察（用户学习数据/正确率分布/留存）
- 系统（用户/角色/设置）

---

## 6. 数据迁移策略

### 6.1 导出脚本（知行侧，一次性）

```bash
# 在 zhengkao-tong 仓库
cd server
source .venv/bin/activate
python -c "
from app.database import SessionLocal
from app.models import Article, Question, WrongAnswer, ManualWrong, KnowledgeNode, RmrbArticle, ShenlunMineLog, ShenlunNormTerm, DushuDailyLog, EnglishArticle
import json, os

db = SessionLocal()
os.makedirs('export', exist_ok=True)

# 1. 文章（公考时政）
with open('export/articles.json', 'w', encoding='utf-8') as f:
    rows = db.query(Article).all()
    json.dump([{...} for r in rows], f, ensure_ascii=False, default=str)

# 2. 题目
# 3. 错题
# 4. 手动错题
# 5. 知识框架
# 6. 申论（人民日报/开采/规范词）
# 注意：不导出 EnglishArticle/Dushu*/Health*/Ledger*/Wealth*

print('导出完成')
"
```

### 6.2 导入脚本（知行公考侧，一次性）

```bash
# 在 zhixing-gongkao 仓库
cd server
source .venv/bin/activate
python scripts/import_from_zhixing.py /path/to/export
```

脚本要点：
- 不导入 ID（让数据库自增），保留外键引用关系用业务键（slug/标题匹配）
- 用户态数据（错题本、签到、积分、计划）**不导入**——新仓从空开始
- 知识框架走 `obsidian/` 目录的 md 同步流程（已有），不直接导 JSON

### 6.3 暂存结构

```
zhengkao-tong/server/export/
├── articles.json
├── questions.json
├── wrong_answers.json
├── manual_wrong.json
├── knowledge_nodes.json
├── rmrb_articles.json
├── shenlun_mines.json
└── shenlun_terms.json
```

---

## 7. 横切能力继承检查表

> 以下能力**全继承**，不重写；M1 仅做替换品牌色/文案。

| 能力 | 源 | 目标 | 检查 |
| --- | --- | --- | --- |
| JWT 鉴权 | `core/security.py` + `utils/auth.ts` | 保留 | ☐ |
| RBAC 权限 | `core/permissions.py` | 保留 | ☐ |
| 积分系统 | `user_service.py` + `store/user.ts` | 保留 | ☐ |
| SRS 记忆曲线 | `services/srs.py` + `utils/memoryCurve.ts` | 保留 | ☐ |
| KaTeX 公式 | `components/LatexBlock.vue` + `utils/latex.ts` | 保留 | ☐ |
| 语音输入 | `components/VoiceInputBtn.vue` | 保留 | ☐ |
| 深色模式 | `store/settings.ts` + `styles/variables.scss` | 保留 + 换色 | ☐ |
| 全局反馈封装 | `utils/feedback.ts` + `utils/feedbackHost.ts` + `platform.ts` | 保留 | ☐ |
| formFlush | `utils/formFlush.ts` | 保留 | ☐ |
| 图片上传 | `utils/media.ts` + `upload_paths.py` | 保留 | ☐ |
| 日期/时区 | `server/app/timezone.py` + 前端 dayjs | 保留 | ☐ |
| Mock 服务 | `src/mock/service.ts` | 保留 | ☐ |

---

## 8. 搬迁检查表（执行时逐行打勾）

> M1 启动后，按此表逐项验收。□ = 未完成 ✅ = 完成 ⚠️ = 偏差需说明

### 8.1 仓库与基础

- ☐ 新仓库 `zhixing-gongkao` 创建、初始 README/LICENSE/.gitignore
- ☐ 知行仓库打 `archive/v1.0-final` tag 并推送
- ☐ 复制基线（src/、server/、配置、docker、scripts、obsidian）
- ☐ `.env.docker` 改名 + 端口调整（如 8001）

### 8.2 删除（6 域 50 页）

- ☐ 1.英语 16 页 + 6 个 service + 16 张表 + seed + 管理后台
- ☐ 2.读书 7 页 + 1 service + 4 张表 + 管理后台
- ☐ 3.健康 7 页 + 1 service + 2 张表 + seed + 管理后台
- ☐ 4.账本 6 页 + 1 service + 3 张表 + 管理后台
- ☐ 5.财富 7 页 + 1 service + 3 张表 + 管理后台
- ☐ 6.充值 1 页 + 1 张表 + seed

### 8.3 新增（6 页 + 3 表 + 1 引擎）

- ☐ 今日驾驶舱页（`pages/today/index.vue`）
- ☐ 倒计时配置页（`pages/today/countdown.vue`）+ ExamCountdown 表
- ☐ 上岸卡片 5 类 + card_service + CardRecord 表
- ☐ 能力雷达图（`pages/today/radar.vue` + growth_service 扩展）
- ☐ 今日精选（`pages/question/today.vue` + quiz_service 扩展，v1 末）
- ☐ 智能组卷（`pages/exam/auto.vue`，v1.5）
- ☐ AI 题目讲解（`services/llm_service.py`，v1.5，默认关闭）
- ☐ 申论 AI 预批改（扩展 llm_service，v1.5）
- ☐ activity_event 表 + 5 个写入点（article/quiz/exam/drill/shenlun/card）

### 8.4 改造

- ☐ 路由：删除 47 个 + 新增 6 个 + Tab 顺序（3→4）+ 窗口样式
- ☐ 主题色：`#D0021B` → `#1E3A5F`，深色模式映射更新
- ☐ 后端 API 注册：注释删除路由 + 新增 countdown/card
- ☐ 管理后台菜单：删 6 个域、重排分组
- ☐ seed.py：去掉已删域初始化
- ☐ store/settings.ts + styles/variables.scss：换主色

### 8.5 数据迁移

- ☐ 知行侧导出脚本（8 个 json）
- ☐ 公考侧导入脚本（articles/questions/knowledge/rmrb）
- ☐ 用户态数据从空开始（错题本/签到/积分/计划不导入）
- ☐ 个人公考数据人工核验

### 8.6 验证

- ☐ `npm run dev:h5` 启动学员端
- ☐ `uvicorn app.main:app` 启动后端
- ☐ 登录注册走通
- ☐ 4 Tab 全部可点开
- ☐ 50 个删除页路由已从 `app.config.ts` 移除（grep 验证）
- ☐ 6 个删除 service 已从 `routes.py` 注释（grep 验证）
- ☐ 冒烟测试 `test_api_smoke.py` 通过

---

## 9. 风险与依赖

| 风险 | 影响 | 对策 | 触发后动作 |
| --- | --- | --- | --- |
| 删除不彻底（如有组件依赖 `EnglishArticle`） | 启动报错 | M1 启动前全局 grep `EnglishArticle/DushuBook/HealthUserState/LedgerExpense/WealthSnapshot/RechargePackage` | 报红即停，按报错链反向删除 |
| `src/api/index.ts` 体积膨胀，新旧接口混杂 | 调试困难 | M1 重构时按业务域拆分到 `src/api/{auth,user,article,question,...}.ts` | 与 M1 同步做 |
| 颜色变量被硬编码（如 `#D0021B` 散落） | 换色不彻底 | 启动后 grep `#D0021B` / `#d0021b` 全文，强制改 CSS 变量 | 验收项"主题色"必须 grep 干净 |
| 知行仓库未冻结，新需求又被提进来 | 范围蔓延 | M1 第 1 天打 archive tag，文档记录冻结事实 | 任何新增需求进新仓 backlog |
| 6 域 seed 数据残留（如默认英语课） | 启动后页面空白 | `seed.py` 检查每个 if 分支，逐个确认是否还需要 | M1 第 3 天验证 seed 流程 |
| `app.config.ts` 路由遗漏（漏删/漏加） | 404/无法进入 | 执行时对比 4.1 / 4.2 列表逐行勾选 | 启动后路由表全量枚举一遍 |
| Obsidian md 同步逻辑依赖英语/读书目录 | 知识框架同步异常 | 同步前检查 `obsidian/` 目录结构，剔除已删模块 | M3 前完成 |
| 管理后台 Element Plus 路由和菜单不同步 | 404 | 删菜单时同步删 `admin-web/src/router/index.ts` | M1 同步做 |
| 个人公考数据导出/导入字段名不一致 | 导入失败 | 导出脚本用 `__table__` 元数据生成字段名，导入端用同套映射 | M1.5 启动前做 |

---

## 10. 验收口径

- **M1 通过**：本地能跑、能登录、能签到、今日页可看、4 Tab 全部可点开、50 个删除页 grep 不到、6 个 service 在 `routes.py` 注释掉
- **M2 通过**：每天能用新应用刷资料分析专项，全流程（含分节进度、出题、错题、统计）跑通
- **M3 通过**：日闭环跑通一天（早看任务 → 中刷题/读时政 → 晚复盘），资料分析外的所有保留域（除能力雷达、上岸卡片）已落地
- **M4 通过**：每天能出一张打卡卡发视频号，activity_event 库能查

---

*本文档配套：总览见 [`README.md`](./README.md)，视觉规范见 [`02-视觉规范.md`](./02-视觉规范.md)，任务拆解见 [`03-里程碑与任务拆解.md`](./03-里程碑与任务拆解.md)，数据模型见 [`04-数据模型与活动事件.md`](./04-数据模型与活动事件.md)，风险见 [`05-风险与开放问题.md`](./05-风险与开放问题.md)。*



